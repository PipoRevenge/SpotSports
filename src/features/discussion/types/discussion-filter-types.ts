/**
 * Discussion feature-specific filter & sort types
 * Re-exports the shared types so the feature can import locally
 */
export type { DiscussionFilters, DiscussionSortField, DiscussionSortOptions } from '@/src/types/filtering.types';


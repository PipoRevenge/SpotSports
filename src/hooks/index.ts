/**
 * Shared hooks
 * Used across features for common functionality
 */

export { useMediaUrl, useMediaUrls } from './use-media-urls';
export { useSportsMapByIds } from './use-sports';
export { useUserLocation } from './use-user-location';

// VoteState type for backward compatibility
export type { VoteState } from './use-vote';

